extension=apcu.so
; Shared memory size
apc.shm_size = <%= node[:lamp_chef][:apc][:memory] %>
; Improve performance in production
; apc.stat = 1
apc.write_lock = 1
; fixes slam defense warnings
apc.slam_defense = <%= node[:lamp_chef][:apc][:slam_defense] ? 'on' : 'off' %>
